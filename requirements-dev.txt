-r requirements.txt
# pandas_ta is only published as a pre-release; it is used as a test oracle only.
# It depends on numba, which does not yet support Python 3.14 - keep it out of the cloud build.
pandas_ta==0.4.71b0
pytest>=8.0
httpx>=0.27
setuptools>=69
